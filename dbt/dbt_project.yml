name: pipeline
version: '1.0.0'
config-version: 2

profile: pipeline

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  pipeline:
    marts:
      +materialized: table
      +schema: marts
